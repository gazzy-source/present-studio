# archive/

Superseded material, kept out of the live tree.

When something is replaced, move it here rather than deleting it. Nothing in this
folder is read at runtime by any engine, adapter or agent.

Preserve the original path when archiving so provenance stays legible, e.g.
`archive/engines/positioning-v1.md`.

Currently empty.
